import 'package:flutter/material.dart';

class ScheduleViewScreen extends StatelessWidget {
  const ScheduleViewScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('스케줄 화면 준비 중'));
  }
}